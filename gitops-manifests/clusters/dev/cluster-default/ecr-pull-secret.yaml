# TODO: Replace .dockerconfigjson with actual ECR credentials, or use
# ecr-credential-helper / IRSA for auto-rotating credentials.
# This manifest is a scaffold — it does not auto-deploy without a cluster ApplicationSet.
apiVersion: v1
kind: Secret
metadata:
  name: ecr-pull-secret
  namespace: rollouts-dev
type: kubernetes.io/dockerconfigjson
data:
  # base64-encoded docker config:
  # {"auths":{"819211779624.dkr.ecr.us-west-2.amazonaws.com":{"auth":"<base64-user:password>"}}}
  .dockerconfigjson: REPLACE_WITH_BASE64_DOCKER_CONFIG
