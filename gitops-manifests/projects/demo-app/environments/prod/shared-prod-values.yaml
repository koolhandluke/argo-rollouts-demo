# gitops-manifests/projects/demo-app/environments/prod/shared-prod-values.yaml
# Manually updated (rollouts-demo) on promotion from staging.
image:
  repository: 819211779624.dkr.ecr.us-west-2.amazonaws.com/demo-app
  tag: "v0.1.0"

rollout:
  strategy: canary

replicaCount: 3

metrics:
  enabled: true

loadgen:
  enabled: true
