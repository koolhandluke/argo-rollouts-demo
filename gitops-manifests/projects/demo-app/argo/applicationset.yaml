apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: rollouts-demo
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/koolhandluke/argo-rollouts-demo.git
        revision: HEAD
        directories:
          - path: gitops-manifests/projects/demo-app/environments/*/*
  template:
    metadata:
      # path[5]=cluster dir name → demo-dev, demo-staging, demo-prod, demo-prod-east
      name: 'demo-{{path[5]}}'
    spec:
      project: rollouts-demo
      source:
        repoURL: https://github.com/koolhandluke/argo-rollouts-demo.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: 'demo-{{path[5]}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
